import SasSelectArea from './src/select-area';

/* istanbul ignore next */
SasSelectArea.install = function(Vue) {
  Vue.component(SasSelectArea.name, SasSelectArea);
};

export default SasSelectArea;
