import * as types from '../../mutation-types'
// const axios = createAxios()
const signList = {
  state: {
    tableData:[],
    pages:0,
    userList:[],
    pagesList:0
  }
};

export default signList;
